import numpy as np

def compute_transmission_rates(src, target):
    #compute src viral load
    #compute target susceptibility to transmission e.g. if they have mask, etc.
    
    #prob_inf    = 
    #exposed and infected targets
    exp_inf     = np.random.choice(target, replace = False)
    
    return exp_inf